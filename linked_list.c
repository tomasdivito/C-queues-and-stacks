#include <stdio.h>
#include <stdlib.h>

struct Node {
  struct Node *next;
  int value;
};

struct Linked_List {
  struct Node *first;
};

void add(struct Linked_List*, int, int);
void delete(struct Linked_List*, int);

int main() {
  

  return 0;
}

void add(struct Linked_List* list, int position, int value) {

}

void delete(struct Linked_List* list, int position) {

}
